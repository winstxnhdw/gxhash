name: Format

on:
  pull_request:
    types: [opened]
  push:
    paths:
      - .github/workflows/formatter.yml
      - "**/pyproject.toml"
      - "**.lock"
      - "**.py"
      - "**.pyi"

permissions:
  contents: write

jobs:
  format:
    uses: winstxnhdw/actions/.github/workflows/format-python.yml@main
